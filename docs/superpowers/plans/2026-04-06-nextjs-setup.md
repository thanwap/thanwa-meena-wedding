# Next.js Setup + Save the Date Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffold a Next.js 15 App Router project inside `web/` and build a fullscreen "Save the Date / Coming Soon" page for frommeenatothanwaforever.com.

**Architecture:** Fully static Server Component page. A fullscreen `next/image` photo sits behind a gradient overlay `<div>`; all wedding details are absolutely positioned at the bottom-center. No `'use client'` directives needed. Vercel will pre-render this as a static HTML page.

**Tech Stack:** Next.js 15 (App Router), Tailwind CSS v4, TypeScript, `next/image`, `next/font/google` (Playfair Display + Geist Sans), Jest + React Testing Library

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `web/package.json` | create | Dependencies and scripts |
| `web/next.config.ts` | create | Minimal Next.js config |
| `web/tsconfig.json` | create | TypeScript config (generated by Next.js) |
| `web/jest.config.ts` | create | Jest config for Next.js |
| `web/jest.setup.ts` | create | @testing-library/jest-dom setup |
| `web/app/globals.css` | create | Tailwind v4 import + base resets |
| `web/app/layout.tsx` | create | Font setup, metadata, viewport, html/body shell |
| `web/app/page.tsx` | create | Save the Date page (Server Component) |
| `web/app/__tests__/page.test.tsx` | create | Render test: verify all text content present |
| `web/public/placeholder.jpg` | create | Grey placeholder image (download via curl) |

---

## Task 1: Scaffold Next.js project in `web/`

**Files:**
- Create: `web/` (directory with full Next.js scaffold)

- [ ] **Step 1: Run create-next-app inside the repo root**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
npx create-next-app@latest web \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*" \
  --yes
```

Expected output: "Success! Created web at .../web"

- [ ] **Step 2: Verify scaffold**

```bash
ls web/app web/public
```

Expected: `app/` contains `layout.tsx`, `page.tsx`, `globals.css`. `public/` exists.

- [ ] **Step 3: Install testing dependencies**

```bash
cd web
npm install --save-dev jest jest-environment-jsdom @testing-library/react @testing-library/jest-dom @types/jest
```

- [ ] **Step 4: Install Playfair Display font (already available via next/font/google — no install needed, just verify next is installed)**

```bash
cd web && npm list next | head -3
```

Expected: `next@15.x.x` listed.

- [ ] **Step 5: Commit scaffold**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/
git commit -m "feat: scaffold Next.js 15 App Router project in web/"
```

---

## Task 2: Configure Jest

**Files:**
- Create: `web/jest.config.ts`
- Create: `web/jest.setup.ts`

- [ ] **Step 1: Create jest.config.ts**

```typescript
// web/jest.config.ts
import type { Config } from 'jest'
import nextJest from 'next/jest.js'

const createJestConfig = nextJest({ dir: './' })

const config: Config = {
  coverageProvider: 'v8',
  testEnvironment: 'jsdom',
  setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],
}

export default createJestConfig(config)
```

- [ ] **Step 2: Create jest.setup.ts**

```typescript
// web/jest.setup.ts
import '@testing-library/jest-dom'
```

- [ ] **Step 3: Add test script to package.json**

Open `web/package.json` and add to the `"scripts"` section:

```json
"test": "jest",
"test:watch": "jest --watch"
```

- [ ] **Step 4: Commit**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/jest.config.ts web/jest.setup.ts web/package.json
git commit -m "feat: add Jest + React Testing Library config"
```

---

## Task 3: Write failing test for the Save the Date page

**Files:**
- Create: `web/app/__tests__/page.test.tsx`

- [ ] **Step 1: Create test directory and test file**

```bash
mkdir -p web/app/__tests__
```

```typescript
// web/app/__tests__/page.test.tsx
import { render, screen } from '@testing-library/react'
import Page from '../page'

describe('Save the Date page', () => {
  it('renders the couple names', () => {
    render(<Page />)
    expect(screen.getByText('Thanwa')).toBeInTheDocument()
    expect(screen.getByText('Meena')).toBeInTheDocument()
  })

  it('renders the wedding date', () => {
    render(<Page />)
    expect(screen.getByText('26 · 12 · 2026')).toBeInTheDocument()
  })

  it('renders the venue', () => {
    render(<Page />)
    expect(
      screen.getByText('The Cop Seminar and Resort, Pattaya')
    ).toBeInTheDocument()
  })

  it('renders the tagline', () => {
    render(<Page />)
    expect(
      screen.getByText('Join us as we celebrate our love')
    ).toBeInTheDocument()
  })

  it('renders the coming soon badge', () => {
    render(<Page />)
    expect(screen.getByText('WEBSITE COMING SOON')).toBeInTheDocument()
  })

  it('renders the Google Maps link with correct href', () => {
    render(<Page />)
    const link = screen.getByRole('link', { name: /view on google maps/i })
    expect(link).toHaveAttribute(
      'href',
      'https://maps.app.goo.gl/WysXSoYYKXm98CcD8'
    )
    expect(link).toHaveAttribute('target', '_blank')
    expect(link).toHaveAttribute('rel', 'noopener noreferrer')
  })
})
```

- [ ] **Step 2: Run test to confirm it fails (page.tsx hasn't been replaced yet)**

```bash
cd web && npm test -- --testPathPattern="page.test" --no-coverage
```

Expected: FAIL — tests fail because the default scaffold `page.tsx` doesn't have our content.

- [ ] **Step 3: Commit the failing test**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/app/__tests__/page.test.tsx
git commit -m "test: add failing tests for Save the Date page content"
```

---

## Task 4: Add placeholder image

**Files:**
- Create: `web/public/placeholder.jpg`

- [ ] **Step 1: Download a grey placeholder photo (1920×1080)**

```bash
curl -L "https://picsum.photos/1920/1080/grayscale" -o web/public/placeholder.jpg
```

Expected: `web/public/placeholder.jpg` exists and is a valid JPEG.

```bash
file web/public/placeholder.jpg
```

Expected output includes: `JPEG image data`

- [ ] **Step 2: Commit**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/public/placeholder.jpg
git commit -m "feat: add placeholder wedding photo"
```

---

## Task 5: Implement layout.tsx

**Files:**
- Modify: `web/app/layout.tsx`

- [ ] **Step 1: Replace layout.tsx with font + metadata setup**

```typescript
// web/app/layout.tsx
import type { Metadata, Viewport } from 'next'
import { Playfair_Display, Geist } from 'next/font/google'
import './globals.css'

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-playfair',
  display: 'swap',
})

const geist = Geist({
  subsets: ['latin'],
  variable: '--font-geist',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Thanwa & Meena — 26.12.2026',
  description:
    'Save the Date — Join us as we celebrate our love. The Cop Seminar and Resort, Pattaya.',
  openGraph: {
    title: 'Thanwa & Meena — Save the Date',
    description: '26 December 2026 · The Cop Seminar and Resort, Pattaya',
    type: 'website',
  },
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${playfair.variable} ${geist.variable}`}>
      <body className="antialiased">{children}</body>
    </html>
  )
}
```

- [ ] **Step 2: Update globals.css**

```css
/* web/app/globals.css */
@import "tailwindcss";

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html,
body {
  height: 100%;
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/app/layout.tsx web/app/globals.css
git commit -m "feat: configure fonts, metadata, and base styles in layout"
```

---

## Task 6: Implement page.tsx (Save the Date)

**Files:**
- Modify: `web/app/page.tsx`

- [ ] **Step 1: Replace page.tsx**

```typescript
// web/app/page.tsx
import Image from 'next/image'

export default function Page() {
  return (
    <main className="relative h-screen w-full overflow-hidden">
      {/* Background photo */}
      <Image
        src="/placeholder.jpg"
        alt="Thanwa and Meena"
        fill
        priority
        className="object-cover object-center"
        sizes="100vw"
      />

      {/* Dark gradient overlay */}
      <div
        className="absolute inset-0"
        style={{
          background:
            'linear-gradient(to top, rgba(0,0,0,0.72) 0%, rgba(0,0,0,0.2) 55%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Content — anchored bottom-center */}
      <div className="absolute inset-0 flex flex-col items-center justify-end pb-10 px-6 text-center text-white">
        {/* Eyebrow */}
        <p
          className="mb-4 text-xs tracking-[0.35em] uppercase"
          style={{ color: 'rgba(255,255,255,0.6)', fontFamily: 'var(--font-geist)' }}
        >
          Save the Date
        </p>

        {/* Names */}
        <h1
          className="text-4xl sm:text-5xl font-light tracking-wide leading-tight"
          style={{ fontFamily: 'var(--font-playfair)' }}
        >
          Thanwa
          <span
            className="block text-2xl sm:text-3xl my-1"
            style={{ color: 'rgba(255,255,255,0.7)' }}
          >
            &amp;
          </span>
          Meena
        </h1>

        {/* Divider */}
        <div
          className="my-4 w-8"
          style={{ height: '1px', background: 'rgba(255,255,255,0.35)' }}
        />

        {/* Date */}
        <p
          className="text-sm tracking-[0.25em]"
          style={{ color: 'rgba(255,255,255,0.9)', fontFamily: 'var(--font-geist)' }}
        >
          26 · 12 · 2026
        </p>

        {/* Venue */}
        <p
          className="mt-1 text-xs tracking-wide"
          style={{ color: 'rgba(255,255,255,0.55)', fontFamily: 'var(--font-geist)' }}
        >
          The Cop Seminar and Resort, Pattaya
        </p>

        {/* Google Maps link */}
        <a
          href="https://maps.app.goo.gl/WysXSoYYKXm98CcD8"
          target="_blank"
          rel="noopener noreferrer"
          className="mt-1 text-xs"
          style={{ color: 'rgba(255,255,255,0.4)', fontFamily: 'var(--font-geist)' }}
        >
          📍 View on Google Maps
        </a>

        {/* Tagline */}
        <p
          className="mt-3 text-xs italic"
          style={{ color: 'rgba(255,255,255,0.5)', fontFamily: 'var(--font-playfair)' }}
        >
          Join us as we celebrate our love
        </p>

        {/* Coming Soon badge */}
        <div
          className="mt-5 px-4 py-1.5 rounded-full text-xs tracking-[0.2em] uppercase"
          style={{
            background: 'rgba(255,255,255,0.1)',
            border: '1px solid rgba(255,255,255,0.2)',
            color: 'rgba(255,255,255,0.7)',
            fontFamily: 'var(--font-geist)',
            backdropFilter: 'blur(4px)',
          }}
        >
          WEBSITE COMING SOON
        </div>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Run tests — all should pass now**

```bash
cd web && npm test -- --testPathPattern="page.test" --no-coverage
```

Expected: PASS — 6 tests passing.

- [ ] **Step 3: Commit**

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add web/app/page.tsx
git commit -m "feat: implement Save the Date page with fullscreen photo layout"
```

---

## Task 7: Verify build and lint

**Files:** No changes — validation only.

- [ ] **Step 1: Run linter**

```bash
cd web && npm run lint
```

Expected: `✔ No ESLint warnings or errors`

- [ ] **Step 2: Run full test suite**

```bash
cd web && npm test -- --no-coverage
```

Expected: All 6 tests PASS.

- [ ] **Step 3: Run production build**

```bash
cd web && npm run build
```

Expected: Output ends with:
```
✓ Compiled successfully
Route (app)         Size
┌ ○ /               ...
```
No errors, no warnings.

- [ ] **Step 4: Smoke-test locally**

```bash
cd web && npm start
```

Open http://localhost:3000 — confirm fullscreen photo, names, date, venue, maps link, tagline, and Coming Soon badge all appear.

- [ ] **Step 5: Commit verification note (no code changes — just update PHASES.md)**

Open `PHASES.md` and check off:
```markdown
- [x] Next.js project setup (App Router)
- [x] "Save the Date: 26/12/2026" page with pre-wedding photo
```

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add PHASES.md
git commit -m "chore: mark Phase 1 Next.js tasks complete in PHASES.md"
```

---

## Task 8: Deploy to Vercel and connect custom domain

**Files:** No code changes — Vercel dashboard + Cloudflare DNS.

- [ ] **Step 1: Import repo to Vercel**

1. Go to https://vercel.com/new
2. Import the `thanwa-meena-wedding` GitHub repo
3. In **Root Directory**, type `web`
4. Framework Preset: **Next.js** (auto-detected)
5. Click **Deploy**

Expected: Deployment succeeds, Vercel provides a `.vercel.app` preview URL.

- [ ] **Step 2: Add custom domain in Vercel**

1. In Vercel project → **Settings → Domains**
2. Add `frommeenatothanwaforever.com`
3. Vercel will show: "Add a CNAME record pointing to `cname.vercel-dns.com`"

- [ ] **Step 3: Add CNAME in Cloudflare**

1. Cloudflare Dashboard → `frommeenatothanwaforever.com` → **DNS**
2. Add record:
   - Type: `CNAME`
   - Name: `@` (or `frommeenatothanwaforever.com`)
   - Target: `cname.vercel-dns.com`
   - Proxy: **Orange cloud ON** (proxied)
3. Save

- [ ] **Step 4: Verify domain is live**

Wait ~2 minutes, then:

```bash
curl -I https://frommeenatothanwaforever.com
```

Expected: `HTTP/2 200` with `x-vercel-id` header present.

- [ ] **Step 5: Update PHASES.md**

```markdown
- [x] Deploy to Vercel, connect custom domain via CNAME in Cloudflare
```

```bash
cd /Users/asol3/thanwap/claude/thanwa-meena-wedding
git add PHASES.md
git commit -m "chore: mark Phase 1 deploy task complete"
```

---

## Phase 1 Complete ✓

All Phase 1 tasks done:
- [x] Next.js project setup (App Router)
- [x] "Save the Date: 26/12/2026" page with pre-wedding photo
- [x] Deploy to Vercel, connect custom domain via CNAME in Cloudflare
